// New Page for Account page
import 'package:flutter/cupertino.dart';

class AccountPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return ListView(
      children: [
        // Display account information or settings here
        Text('Account Page'),
      ],
    );
  }
}